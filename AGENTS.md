# dewey-mcp

## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `KevoHoff/dewey-mcp` using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default five-label triage vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: root `CONTEXT.md` plus root `docs/adr/`. See `docs/agents/domain.md`.
