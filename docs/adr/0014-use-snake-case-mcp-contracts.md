# Use snake_case MCP Contracts

Dewey MCP uses `snake_case` field names in public MCP request and response models. This keeps the MCP contract aligned with the Python/Pydantic model layer and avoids adding casing aliases unless a real client requires them later.
